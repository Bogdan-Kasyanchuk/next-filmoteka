const parameters = {
    API_KEY: process.env.NEXT_PUBLIC_API_KEY,
    BASE_URL: process.env.NEXT_PUBLIC_BASE_URL,
    LOCALE: process.env.NEXT_PUBLIC_LOCALE,
};

export const { API_KEY, BASE_URL, LOCALE } = parameters;