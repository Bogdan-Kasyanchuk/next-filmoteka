const parameters = {
    API_KEY: process.env.NEXT_PUBLIC_API_KEY,
    BASE_URL: process.env.NEXT_PUBLIC_BASE_URL,
    BASE_URL_POSTER: process.env.NEXT_PUBLIC_BASE_URL_POSTER,
    BASE_URL_BACKDROP: process.env.NEXT_PUBLIC_BASE_URL_BACKDROP,
    LOCALE: process.env.NEXT_PUBLIC_LOCALE,
    ADULT: process.env.NEXT_PUBLIC_ADULT,
};

export const {
    API_KEY,
    BASE_URL,
    BASE_URL_POSTER,
    BASE_URL_BACKDROP,
    LOCALE,
    ADULT
} = parameters;